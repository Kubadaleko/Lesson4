// 32. Задать массив из 8 целых элементов и вывести их на экран


int[] a = { 1, 2, 3, 5, 4, 6, 9, 8 };
foreach (int el in a)
    System.Console.Write($"{el} ");

// или 

int[] a = new int[8];
foreach (int el in a)
    System.Console.Write($"{el} ");

